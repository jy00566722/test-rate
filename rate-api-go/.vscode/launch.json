{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto", // "debug" or "run" can be specified if needed
            "program": "${fileDirname}", // or "${workspaceFolder}" to specify the root
            "env": {},
            "args": [],
            "cwd": "${workspaceFolder}" // 指定工作目录为工作区根目录
        }
    ]
}
